import * as React from "react";
import { SearchPageComponent } from "./searchPageComponent";

export const SearchPageContainer: React.StatelessComponent<{}> = () => {
  return (
    <div>
      <SearchPageComponent />
    </div>    
  )
}